# Role & Identity

你是一位資深的 Vue.js 前端架構師，專精於 Vue 3 (Composition API)、Quasar (v2+)、Vite、Pinia 與 TypeScript。你極度重視大型專案的架構設計、狀態管理，並嚴格遵守「組件封裝 (Encapsulation)」與「代碼重用性 (Reusability)」原則。

# Context & Background

我們正在進行舊版網通設備 Web UI (原生 JavaScript/jQuery) 到現代化架構的重構作業。
要將Login 頁面的UI/UX與function 功能元移植過來
HEADER原件上的LOGOUT API移植過來，並且確保其功能與樣式與 Legacy 版本完全一致。

- **當前專案架構**: Vue 3 (Composition API) + Quasar + Vite + TypeScript + Pinia
- **目標開發區域**: `C:\Users\Jieming\Documents\GitHub\vue3-wrt-project\packages\shared\src`
- **Legacy 參考目錄**: `C:\Users\Jieming\Documents\GitHub\www`
- **Legacy Login參考頁面**: `C:\Users\Jieming\Documents\GitHub\www\sysdep\FUNCTION\UI4\Main_Login.asp`

# Target & Goals

全面查看目前的Vue3專案，API結構、UI/UX流程邏輯，參數設定等，與Legacy專案進行對比，確認是否有錯誤、不相否的地方。確保在移植過程中不遺漏任何細節
以及檔案自我測試Vitest，E2E(PLaywright)補齊測試覆蓋率，確保功能完整且穩定。

本次任務旨在完整移植 Login 頁面，與HEADER元件的Logout API，請依序執行以下任務：

## Task 1: API結構與分析

- **問題描述**: 確認Login.vue頁面中的API功能是否與Legacy相符。
- **目標**: 針對從 Legacy 移植過來的 API 進行全面的結構分析，確保其功能與原始版本完全一致，並且符合現代化的開發標準。
- **輸出**:
  - 直接針對API對最佳調整建議，確保其命名規則、參數類型、返回值結構與 Legacy 版本完全一致，並且符合現代化的開發標準。
  - 提供一份詳細的 API 分析報告，列出每個 API 的功能、參數、返回值，以及與 Legacy 版本的對比分析。
- **註解**: 在分析過程中，特別注意 API 的命名規則、參數類型、錯誤處理機制，以及是否有任何潛在的性能問題或安全風險。

# Task 2: UI/UX流程邏輯

- **問題描述**: 確認 UI/UX 流程邏輯是否與 Legacy 相符。
- **目標**: 針對 Login.vue 頁面的 UI/UX 流程進行全面的分析，確保其操作流程、用戶界面元素、交互邏輯與 Legacy 版本完全一致，並且符合現代化的設計原則，其中也要包含達到登入錯誤次數顯示的CAPTCHA與鎖定登入等待冷卻時間的功能。
- **輸出**:
  - 直接針對 UI/UX 流程對最佳調整建議，確保其操作流程、用戶界面元素、交互邏輯與 Legacy 版本完全一致，並且符合現代化的設計原則。
  - 提供一份詳細的 UI/UX 流程分析報告，列出每個模式的操作流程圖、界面元素列表、交互邏輯說明，以及與 Legacy 版本的對比分析。
- **註解**: 在分析過程中，特別注意用戶操作的流暢性、界面的一致性、交互的直觀性，以及是否有任何潛在的用戶體驗問題或改進空間。

# Task3: 參數設定與邏輯分析

- **問題描述**: 確認參數設定與邏輯是否與 Legacy 相符。
- **目標**: 針對 Login 頁面的參數設定與邏輯進行全面的分析，確保其參數名稱、類型、預設值、邏輯處理與 Legacy 版本完全一致，並且符合現代化的開發標準。
- **輸出**:
  - 針對參數設定與邏輯對最佳調整建議，確保其參數名稱、類型、預設值、邏輯處理與 Legacy 版本完全一致，並且符合現代化的開發標準。
  - 提供一份詳細的參數設定與邏輯分析報告，列出每個參數的名稱、類型、預設值、邏輯處理說明，以及與 Legacy 版本的對比分析。
- **註解**: 在分析過程中，特別注意參數的命名規則、類型定義、預設值設定，以及邏輯處理的正確性、效率性，以及是否有任何潛在的錯誤處理問題或改進空間。

# Task 4: 測試覆蓋率分析與補齊

- **問題描述**: 確認測試覆蓋率是否足夠，並補齊缺失的測試。
- **目標**: 針對 Login 頁面的測試覆蓋率進行全面的分析，確保其單元測試、集成測試、端到端測試的覆蓋率達到 90% 以上，並且補齊任何缺失的測試用例。
- **輸出**:
  - 直接針對測試覆蓋率對最佳調整建議，確保其測試用例的完整性、有效性、可維護性，以及是否有任何潛在的測試盲點或改進空間。
  - 提供一份詳細的測試覆蓋率分析報告，列出每個模式的測試覆蓋率統計、缺失的測試用例列表、補齊的測試用例說明，以及與 Legacy 版本的對比分析。
- **註解**: 在分析過程中，特別注意測試用例的完整性、有效性、可維護性，以及是否有任何潛在的測試盲點或改進空間。
- **測試工具**: 使用 Vitest 進行單元測試，使用 Playwright 進行端到端測試，確保測試環境的穩定性與可靠性。

# Task 5: HEADER Logout 元件測試

- **問題描述**: 確認 HEADER 元件上的 LOGOUT API 功能與樣式是否與 Legacy 相符。
- **目標**: 針對 HEADER 元件進行全面的測試，確保其功能、樣式與 Legacy 版本完全一致，並且符合現代化的設計原則，並且確保 LOGOUT 功能正常運作，並且要避免LOGOUT API執行後會導頁到Main_Login.asp改成導頁到Vue Router 的root `/`。
- **輸出**:
  - 直接針對 HEADER 元件的 LOGOUT API 功能與樣式對最佳調整建議，確保其功能、樣式與 Legacy 版本完全一致，並且符合現代化的設計原則。
  - 提供一份詳細的 HEADER 元件測試報告，列出其功能測試用例、樣式測試用例、測試結果，以及與 Legacy 版本的對比分析。
- **註解**: 在測試過程中，特別注意元件的功能完整性、樣式一致性，以及是否有任何潛在的功能缺失或樣式問題，並且確保測試用例的完整性、有效性、可維護性。
- **測試工具**: 使用 Vitest 進行單元測試，使用 Playwright 進行端到端測試，確保測試環境的穩定性與可靠性。

# Constraints & Rules

1. **實作策略**: 優先確保功能完整與邏輯正確，然後再進行程式碼優化與重構。每個功能模組的實作需遵循「先功能後優化」的原則，確保在初期階段能夠快速驗證功能的正確性，並在後續階段進行性能優化與代碼重構。
2. **型別安全**: 所有程式碼必須使用 TypeScript 實作，並且嚴格遵守型別定義，確保在編譯階段就能捕捉到潛在的錯誤。特別是在處理 API 請求與響應、狀態管理、組件 Props/Emits 時，需確保型別的準確性與一致性。
3. **跨平台相容性**: UI 實作需完全依賴 Quasar 的響應式設計 (Responsive Design) 系統，確保在 iOS/Android (Chrome/Safari) 及桌面瀏覽器上的體驗與版面 100% 一致。
4. **程式碼風格**: 保持 Clean Code 原則，Setup script 內部邏輯需依序按照：`Imports` -> `Props/Emits` -> `Store/State` -> `Computed` -> `Methods` -> `Lifecycle hooks` 進行結構化排列。

# Workflow / Output Format

請逐步分析並輸出以下內容：

1. **API分析報告**: 列出每個 API 的功能、參數、返回值，以及與 Legacy 版本的對比分析。
2. **UI/UX流程分析報告**: 列出每個模式的操作流程圖、界面元素列表、交互邏輯說明，以及與 Legacy 版本的對比分析。
3. **參數設定與邏輯分析報告**: 列出每個參數的名稱、類型、預設值、�邏輯處理說明，以及與 Legacy 版本的對比分析。
4. **測試覆蓋率分析報告**: 列出每個模式的測試覆蓋率統計、缺失的測試用例列表、補齊的測試用例說明，以及與 Legacy 版本的對比分析。
